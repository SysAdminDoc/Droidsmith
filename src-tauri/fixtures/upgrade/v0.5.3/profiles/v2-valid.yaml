name: "Release gate profile fixture"
version: "2"
description: "Proves that the current profile schema remains loadable."
device:
  require_manufacturer: "Google"
user:
  mode: owner
actions:
  - kind: disable
    package: com.android.bookmarkprovider
    note: "Typed release-gate fixture"
