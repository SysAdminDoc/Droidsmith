{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Scoped capability set for the Droidsmith main window. Privileged Android tooling flows through Rust #[tauri::command] handlers rather than renderer-launched shell commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
